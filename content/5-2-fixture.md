# Story 5.2 Fixture

This page is a committed test fixture for Story 5.2 (Follow / Feed).

It verifies that a new `.md` page added to the vault surfaces as a new `<item>` in `/feed.xml` after rebuild (AC2 new-page-surfaces test).

Do not remove this file — it is pinned by `web/tests/5-2-follow-feed.spec.ts`.
