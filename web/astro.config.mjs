// @ts-check
import { defineConfig } from 'astro/config';

// Minimal config for the walking skeleton (Story 1.1).
// remark/rehype (GFM), Shiki, and the GitHub-style theme arrive in Epic 2.
export default defineConfig({});
