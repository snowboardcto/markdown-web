# AC Trace Report — Story 1.2: Provision Azure hosting via IaC (Bicep)

Generated by the enhanced-automated-sprint pipeline (Step 10). Infrastructure story — verification is
compile + live-deploy assertions against Azure, not a unit-test suite.

## AC → Verification matrix

| AC | Requirement | Verification | Result |
|----|-------------|--------------|--------|
| AC1 | Templates compile clean | `az bicep build --file infra/main.bicep` → exit 0, **warning-free** (BCP037 phantom-property warnings removed in review fix) | ✅ PASS |
| AC2 | SWA Free, RG-scoped, single resource | `targetScope='resourceGroup'`; `az staticwebapp show` → `swa-markdown-web`, sku Free, East US 2; `az staticwebapp list -g rg-markdown-web` length = 1 | ✅ PASS |
| AC3 | `defaultHostname` emitted as non-empty `*.azurestaticapps.net` output | Deploy output `defaultHostname = purple-pond-09fadd20f.7.azurestaticapps.net`; `curl` → HTTP 200 | ✅ PASS |
| AC4 | Idempotent re-run (no drift, no duplicate) | `az deployment group create` run 3×: Succeeded each time, identical hostname, resource count stays 1. Residual `what-if` Modify = read-only `stableInboundIP` only (un-settable; documented benign noise) | ✅ PASS |
| AC5 | Parameterized, secret-free, no repo linkage | `appName`/`location`/`sku` params w/ defaults; no subscription IDs, no secrets, no `repositoryUrl`/`repositoryToken`; manual-deploy mode | ✅ PASS |

## Live resource provisioned
- `Microsoft.Web/staticSites/swa-markdown-web` in `rg-markdown-web` (East US 2, Free).
- Hostname: `purple-pond-09fadd20f.7.azurestaticapps.net` (HTTP 200, default page until Story 1.3 deploys content).

## Coverage gaps
- None for the 5 ACs. Deployment-token retrieval + `deploy-web.yml` are Story 1.3; custom domain is Story 1.4.

## Code review
- Verdict PASS WITH ITEMS (0 critical, 2 high). All high/med items fixed; 2 low items consciously deferred (documented).

**Trace verdict: 5/5 ACs satisfied. SWA is live.**
