# Story 2.4: Media embedding

Status: ready-for-dev

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As an author,
I want images and video to render inline,
so that my pages are rich, not text-only.

## Acceptance Criteria

1. **Given** a content page containing a relative image embed — the epic example `![](media/powder.jpg)` on `content/x.md` (page `/x`), with the asset present in the vault at `content/media/powder.jpg` — **When** the page is built and viewed **Then** the rendered HTML contains an `<img>` whose `src` resolves to a **served URL** (a root-absolute `/media/powder.jpg`, NOT the literal relative `media/powder.jpg` which would 404 against the page route `/x`), the asset is actually present in the build output and served (HTTP 200, correct image `Content-Type`), and the image displays inline (a non-zero rendered size in the browser). The rewrite happens at **build time** and the emitted HTML is plain static `<img src="/…">` — no `client:*` island, no runtime JS (FR-5/FR-7 preserved). *(AC1 — relative image embed renders inline AND its asset is served from the vault path; FR-3, EXPERIENCE.md line 77 "working links and inline media")*

2. **Given** relative media `src` values, resolved against the **current page's own route/dir** exactly as Story 2.3 resolves relative `.md` links (the SAME `pathToSlug`/`path.posix` resolution rules, in slug space), **When** the page renders **Then**: a same-dir embed `![](media/powder.jpg)` from `/x` → `src="/media/powder.jpg"`; a nested-page embed resolves against the page's directory — from a page at `/sub/page`, `![](../media/powder.jpg)` → `/media/powder.jpg` and `![](pic.png)` → `/sub/pic.png`; a leading `./` is collapsed (`./media/powder.jpg` from `/x` → `/media/powder.jpg`); spaces/percent-encoding in the path are handled deterministically and decoded **per segment** so an encoded separator cannot manufacture a new path part (mirroring 2.3's F1 guard) — and the asset is copied to / served at the **same** resolved path. **Media paths are NOT github-slugged** — unlike `.md`→route slugs, an asset filename must be preserved **byte-for-byte** (case and all) so `Powder Day.JPG` stays `Powder%20Day.JPG`/the real on-disk name and the served file matches the `src` exactly (a slugged `src` would point at a file that does not exist). Directory segments are likewise preserved verbatim. *(AC2 — relative media resolution honours nested dirs / `..` / `./` / encoded chars, resolved against the current page's location, with asset filenames preserved exactly; FR-3)*

3. **Given** the same render path, **non-relative** and **non-media** references are left exactly as authored **When** the page renders **Then** an absolute external media URL (`![](https://example.com/a.png)`, a `data:` URI, a protocol-relative `//host/a.png`, or a root-absolute `/already/served.png`) keeps its `src` unchanged (external/already-served assets are not rewritten and not copied), and the rewrite applies to media elements only — `<img>`, `<video>`, `<audio>`, and their nested `<source>`/`poster` — leaving ordinary `<a href>` links to the Story 2.3 plugin (this story does **not** re-handle `.md` link rewriting). Only *relative* `src`/`poster` values are rewritten. *(AC3 — only relative media src/poster is rewritten; external/data/absolute pass through; `<a>` links remain 2.3's concern; FR-3, scope boundary vs 2.3)*

4. **Given** an image embed with author-provided alt text — `![a skier in deep powder](media/powder.jpg)` — **When** the page renders **Then** the `<img>` carries `alt="a skier in deep powder"` (alt text passes through untouched; the src rewrite must not drop or alter `alt`, `title`, `width`, `height`, or any other authored attribute), and an empty-alt embed `![](media/powder.jpg)` emits `alt=""` (decorative — the standard markdown→HTML behaviour, preserved). Accessibility floor (NFR-6 / UX-DR9): alt text is the author's accessibility affordance and must survive the rewrite. *(AC4 — alt/title/other attributes pass through unchanged; FR-3, NFR-6, UX-DR9)*

5. **Given** non-image media — a video embed authored as raw HTML in markdown (e.g. `<video src="media/clip.mp4" controls></video>` or `<video controls><source src="media/clip.mp4" type="video/mp4"></video>`), since GFM markdown has no native video syntax — **When** the page renders **Then** the relative `src`/`poster` on `<video>`/`<audio>`/`<source>` is rewritten to its served `/…` path the same way images are, the media file is copied into the build output, and it is served with the correct `Content-Type` (e.g. `video/mp4`). Per EXPERIENCE.md, the WEB path serves standard HTML `<video>`/`<img>`; native-client video via MediaElement is Epic 3's concern and is **out of scope** here. *(AC5 — relative `<video>`/`<audio>`/`<source>`/`poster` assets are served too, not just images; FR-3; scope: web only, not native MediaElement)*

6. **Given** a relative media embed whose asset does **not** exist in the vault — `![](media/missing.jpg)` — **When** the page builds **Then** the build does **not crash** and does **not silently exit-0 with a broken page**: the `<img>` still rewrites to its would-be served path (`/media/missing.jpg`) and, because no such file is copied, the browser/crawler gets a normal **404 for that asset** (a broken-image state, never a build crash or a thrown exception) — consistent with Story 2.3's "missing target → clear not-found, never a crash" philosophy. The decision on whether to additionally **fail the build loudly** on a referenced-but-missing asset is a Dev decision documented in the Dev Agent Record (see "Missing-asset policy" in Dev Notes); the floor is *never crash mid-render and never emit a malformed page*. *(AC6 — missing asset → broken-image/404, never a build crash; FR-3, mirrors 2.3 AC4 robustness)*

7. **Given** the media-embedding changes are purely additive to the Story 2.1/2.2/2.3 render path **When** the site builds and the full Playwright suite runs **Then** the build still exits 0 and emits one page per `content/**/*.md`, every page stays server-rendered / JS-free / semantically well-formed (single `<html lang>`→`<head>`→`<body>`→`<main>`/`<article>`, one `<h1>`, doctype), the GitHub theme is unchanged, Story 2.3's link rewriting + custom 404 still work, and **all 66 existing Playwright specs (`web/tests/*.spec.ts`) still pass unchanged** alongside the new media specs. The asset-copy step must not change route emission, slug collision detection, or the content collection. *(AC7 — no regression to 2.1 crawlable/JS-free, 2.2 theme, or 2.3 linking/404; FR-5, FR-7, NFR-3)*

> Source: [_bmad-output/planning-artifacts/epics.md#Story 2.4: Media embedding] (lines 227–237): the epic AC is — **Given** `![](media/powder.jpg)` in a page with the asset in the vault, **When** the page renders, **Then** the image appears inline and the asset is served from the vault path (FR-3). **AC1** is the epic AC verbatim (render inline + asset served from vault path). **AC2** is the derived **resolution-correctness** bar — the epic shows only the same-dir case; architecture.md (#Cross-Cutting 4 line 63 "media resolution"; #Rendering Pipeline line 104 "resolve relative paths against the vault") and Story 2.3's relative-resolution precedent require nested-dir/`..`/`./`/encoded handling, made explicit and testable. **AC3** is the derived **scope/pass-through** bar — external/data/absolute media must not be rewritten or copied, and `.md` links stay 2.3's job. **AC4** is the derived **alt-text/attribute-passthrough** bar (NFR-6/UX-DR9 accessibility floor). **AC5** is the derived **non-image media** bar — FR-3 says "images/video"; EXPERIENCE.md line 77 says "inline media"; video on web is standard HTML `<video>`. **AC6** is the derived **missing-asset robustness** bar, mirroring 2.3 AC4. **AC7** is the derived **regression guardrail** — 2.4 is additive to 2.1/2.2/2.3 and the 66 existing tests must not break.

## Tasks / Subtasks

- [ ] **Task 1 — Write a rehype plugin that rewrites relative media `src`/`poster` to served URLs** (AC: 1, 2, 3, 4, 5)
  - [ ] Create `web/src/lib/rehype-md-media.mjs` (NEW — a small local rehype plugin, sibling to the existing `web/src/lib/rehype-md-links.mjs`; `.mjs` so `astro.config.mjs` can `import` it directly). It is a `unified`/rehype transformer: `export default function rehypeMdMedia() { return (tree, file) => { … } }`. Use `unist-util-visit` (already resolvable in `web/node_modules` — confirmed used by `rehype-md-links.mjs`) to visit every `element` node whose `tagName` is `img`, `video`, `audio`, or `source`. [Source: web/src/lib/rehype-md-links.mjs (the sibling pattern to mirror); architecture.md line 104 "Images/media (FR-3): resolve relative paths against the vault"]
  - [ ] For each media element, read the relative URL attribute(s): `node.properties.src` for `img`/`video`/`audio`/`source`, and ALSO `node.properties.poster` on `<video>` (a poster is a served image too). Rewrite each independently. Do NOT touch `srcset` for the floor (note it in the Dev Agent Record as a known not-yet-handled case — markdown `![]()` never emits `srcset`, and authored `srcset` is an edge case beyond the epic AC). [Source: AC4 poster; HTML media element attributes]
  - [ ] **Media-element attribute coverage matrix (apply the rewrite to ALL of these, independently per attribute):** `<img src>`, `<video src>`, `<video poster>` (BOTH on the same `<video>` — rewrite each, a video can have a relative `src` AND a relative `poster`), `<audio src>`, `<source src>` (nested inside `<video>`/`<audio>` — `<source>` is a sibling-of-content element, visited as its own `element` node, so it is handled by the same `tagName === 'source'` branch; do NOT rely on recursing into the parent). A `<video>` with NO `src` but multiple `<source>` children is the common authored form (`<video controls><source src="a.webm"><source src="a.mp4"></video>`) — each `<source>` is rewritten independently; the `<video>` itself has nothing to rewrite (no `src`, maybe a `poster`). Confirm `<track src>` (captions/subtitles, a relative `.vtt`) is EITHER covered the same way OR explicitly noted as not-yet-handled in the Dev Agent Record (it is a served relative asset too; covering it is low-cost and consistent, but it is beyond the epic's image/video wording — DECIDE and document). [Source: AC3/AC5 `<img>/<video>/<audio>/<source>/poster`; HTML media DOM]
  - [ ] **Attribute value shape defensiveness:** `node.properties.src`/`poster` may be a non-string (HAST can carry array/space-separated property values for some attributes) or `undefined`/empty — guard `typeof v === 'string' && v !== ''` before processing (mirror `rehype-md-links.mjs` line 108). A media element with no relevant attribute is skipped cleanly. Never assume the property exists. [Source: web/src/lib/rehype-md-links.mjs line 108]
  - [ ] **Pass through unchanged (do NOT rewrite, do NOT copy)** when the value is missing/empty, starts with a scheme (`/^[a-z][a-z0-9+.-]*:/i` — matches `http:`, `https:`, `data:`, etc.), is protocol-relative (`//…`), or is root-absolute (`/…`). These are external / already-served / data-URI assets (AC3). Reuse the EXACT pass-through guard shape from `rehype-md-links.mjs` lines 112–119 for consistency. [Source: web/src/lib/rehype-md-links.mjs lines 112–119; AC3]
  - [ ] For a **relative** value: split off any `#fragment`/`?query` is generally N/A for assets, but be defensive — strip a trailing `?query` from the path used to resolve/copy if present (the served file is the path part). Then **decode per segment** (split on `/` FIRST, then `decodeURIComponent` each segment, reject if a decoded segment introduces a new `/` or looks absolute/scheme) — copy the F1 hardening from `rehype-md-links.mjs` lines 130–160 verbatim, since the same `%2F`/`%2E` smuggling risk applies. A malformed `%`-escape leaves the value UNREWRITTEN (degrade, never throw). [Source: web/src/lib/rehype-md-links.mjs lines 121–160 (F1 per-segment decode + separator/scheme guards); AC2]
  - [ ] Resolve the relative asset path **against the current page's directory** in POSIX/slug space, using the SAME page-dir derivation as `rehype-md-links.mjs` lines 100–103: derive `pageDirSlug` from the VFile source path (relative to `contentDir`, drop the basename, then `pathToSlug` the directory) so resolution matches the page's actual route location. `path.posix.join(pageDir, relPath)` + `path.posix.normalize`, then apply the SAME `..`-escape guard (`joined === '..' || joined.startsWith('../')` → leave unrewritten, no clamp) and leading-`/` guard. **CRITICAL DIFFERENCE FROM 2.3:** after resolving the directory path, do **NOT** `pathToSlug` the asset's own filename or path segments — github-slugging would rename the file away from what exists on disk. The served `src` and the copied file must use the **verbatim, on-disk relative path** (only `./`/`..`/nested-dir resolution is applied; no case-folding, no slugging). Set `node.properties.src = '/' + resolvedRelPath` (root-absolute served path). [Source: web/src/lib/rehype-md-links.mjs lines 100–103, 162–193; AC2 "filenames preserved byte-for-byte"]
  - [ ] **VFile path contract + degrade-never-crash:** reuse the `resolveSourcePath(file)` pattern from `rehype-md-links.mjs` lines 56–71 (fallback chain `file.path` → `file.history[0]` → Astro frontmatter seam, validated to be inside `contentDir`). If no usable vault path, leave ALL media on that page unrewritten and `console.warn` once (mirror lines 76–90), so a future Astro VFile-shape change is a visible no-op, not a silent break. Factor `resolveSourcePath`/`contentDir`/page-dir derivation so the media and link plugins stay consistent — consider extracting the shared VFile→pageDirSlug helper into `web/src/lib/slug.mjs` or a new `web/src/lib/page-path.mjs` and importing from BOTH plugins (preferred, to prevent drift); otherwise copy verbatim with a comment pointing at the canonical source. [Source: web/src/lib/rehype-md-links.mjs lines 48–90; AC1/AC2; 2.3 anti-drift precedent (shared slug.mjs)]
  - [ ] Keep the plugin pure: no network. It MAY read the filesystem only to record which relative assets were referenced (for the copy step / missing-asset policy) — but the actual file COPY belongs in Task 2 (an Astro build hook), not inside the rehype transform (rehype runs per-page during render; copying is a once-per-build concern). The plugin's job is the `src` rewrite + (optionally) collecting the set of referenced asset paths. Add a header comment documenting the resolution rules, the no-slug-on-filename rule, and that this is the web half of FR-3 media (the native client resolves the same against the vault at render time in Epic 3 Story 3.5). [Source: architecture.md line 63 "media resolution … build time (HTML) vs render time (native)", line 104; AC1]

- [ ] **Task 2 — Serve vault media: copy `content/`'s non-`.md` assets into the build output** (AC: 1, 5, 6, 7)
  - [ ] The deployed artifact root is `web/dist` (`.github/workflows/deploy-web.yml` line 59 `app_location: 'web/dist'`, `skip_app_build: true`). Azure SWA serves files from that root. Today the content collection copies ONLY `.md` (via `glob({ base: contentBase, pattern: '**/*.md' })` in `web/src/content.config.ts`) — so `content/media/**` is **never** in `dist` and a `<img src="/media/powder.jpg">` would 404. This story must get the vault's non-`.md` assets into `dist` at the SAME relative path the rewritten `src` points to. [Source: deploy-web.yml line 59; web/src/content.config.ts lines 9–13; architecture.md line 143 "content/ is the single source of truth … No content in code"]
  - [ ] **Chosen approach — build-time static copy via an Astro integration hook (NOT Astro `public/`).** Add a tiny inline Astro integration in `web/astro.config.mjs` (or a small module `web/src/lib/copy-vault-media.mjs` imported as an integration) that hooks `astro:build:done` (fires after `dist/` is written, exposes the `dir` output URL) and copies every non-`.md`, non-dotfile file from repo-root `content/**` into `dist/**` at the same relative path (so `content/media/powder.jpg` → `dist/media/powder.jpg`, served at `/media/powder.jpg`). Use `node:fs` (`cpSync`/recursive readdir) + `node:url`/`node:path` (resolve `content/` as `new URL('../content', import.meta.url)` from the config, same pattern as `content.config.ts` line 9). Exclude `.md` (those are pages), `.gitkeep`/dotfiles, and `README.md`. [Source: Astro `astro:build:done` integration hook; web/src/content.config.ts line 9 contentBase resolution; deploy-web.yml dist artifact]
  - [ ] **Why NOT Astro `public/`:** `web/public/**` is copied verbatim into `dist`, but putting media there would require the vault's media to live under `web/public/media/` — duplicating content into `web/`, which violates the architecture's hard rule "`content/` is the single source of truth … No content in code" (architecture.md line 143) and would desync from the vault. A symlink `web/public/media → ../../content/media` is fragile across OSes/CI and Astro may not follow it. The build-time copy keeps `content/` canonical and is deterministic on every OS/CI. Document this decision in the Dev Agent Record. [Source: architecture.md line 143; Astro public/ behavior; 2.3 precedent for choosing the simplest robust static approach]
  - [ ] **Path-coherence invariant:** the copy step and the rehype `src` rewrite MUST agree on the served path. Both resolve to the asset's path **relative to `content/`**, verbatim (no slugging), root-absolutised with a leading `/`. Verify by example: `content/media/powder.jpg` copies to `dist/media/powder.jpg` AND the rewrite emits `/media/powder.jpg`. Add a comment in BOTH the copy hook and the rehype plugin cross-referencing this invariant so they never drift. [Source: AC1, AC2; coherence with deploy artifact]
  - [ ] **Missing-asset policy (AC6):** the copy step copies only files that EXIST in `content/`. A referenced-but-missing asset (`media/missing.jpg`) simply has no file copied → the rewritten `/media/missing.jpg` 404s (broken image, never a build crash). Decide and DOCUMENT whether to additionally emit a build warning (recommended — list referenced-but-missing assets, like 2.3's `unresolvedPages` warn) or fail the build. The non-negotiable floor: the copy and the rewrite must NEVER throw mid-build on a missing asset. Default recommendation: **warn, do not fail** (consistent with 2.3's never-crash-on-broken-target philosophy), but record the choice. [Source: AC6; web/src/lib/rehype-md-links.mjs lines 84–88 (warn-on-degrade precedent)]
  - [ ] Confirm the copy does not clobber or duplicate the page HTML (skip `.md`; do not copy into route directories in a way that collides with `index.html`). Media lives under `content/media/**` and other non-`.md` paths; pages live at `dist/<slug>/index.html`. A `content/media/powder.jpg` → `dist/media/powder.jpg` does not collide with any page route. [Source: web/src/pages/[...slug].astro route emission; AC7]
  - [ ] **Copy-step failure modes — robustness hardening (the `astro:build:done` hook must be deterministic and never throw):**
    - [ ] **Collision / overwrite with emitted HTML or `public/` output:** `astro:build:done` fires AFTER `dist/` is written, so a copied asset could overwrite an already-emitted file (e.g. a `content/x/` directory asset landing on `dist/x/index.html`, or an asset path colliding with a `public/` file already in `dist`). DECIDE the policy: skip-if-exists (do NOT clobber Astro's HTML/`public` output) vs overwrite. Recommended: **never overwrite an existing `dist` file that Astro wrote** (HTML/`public`) — if a vault asset path collides with a route/`public` path, `console.warn` and skip (a content-vs-route path clash is an authoring error, not a crash). Copy is idempotent across re-runs (re-copying the same asset to the same path is a no-op-equivalent overwrite of identical bytes — fine). [Source: AC7 "must not change route emission"; Astro `astro:build:done` ordering]
    - [ ] **Dotfiles / non-asset files:** exclude `.md` (pages), all dotfiles (`.gitkeep`, `.DS_Store`, `.gitignore`), `README.md`/`README` (already covered by `.md`, but exclude any `README*`), and editor/OS cruft (`Thumbs.db`, `*.swp`). Do NOT copy these into the served `dist`. A `content/media/.gitkeep` must NOT appear at `/media/.gitkeep`. [Source: content/media/ holds only .gitkeep today]
    - [ ] **Case-only-difference filenames:** two assets differing only in case (`content/media/Logo.png` + `content/media/logo.png`) are distinct on a case-sensitive FS (Linux CI / Azure SWA) but COLLIDE on a case-insensitive FS (macOS/Windows dev). The copy must NOT silently merge/drop one. Preserve verbatim names (matching the no-slug rewrite rule); if the host FS cannot hold both, that is a dev-environment limitation to `console.warn` about, not a silent data-loss — but do not crash. Note: Linux CI (the deploy path, deploy-web.yml) IS case-sensitive, so both survive in the real artifact. [Source: AC2 verbatim filenames; deploy-web.yml Linux runner]
    - [ ] **Large / binary files:** the copy must be a faithful BINARY copy (no text/encoding transform, no newline normalisation) — use `fs.cpSync`/`copyFileSync` (byte copy), never read-as-utf8-then-write. A multi-MB image/video must copy intact (byte-identical; verify size matches). No streaming/back-pressure concern for a build-time `cpSync`, but do not load the whole tree into memory — copy file-by-file or use `cpSync(recursive)` which streams. [Source: AC5 video bytes; faithful-copy invariant Decision 2]
    - [ ] **Content-Type by extension (serving correctness):** the served `Content-Type` is set by Azure SWA / the preview server from the file EXTENSION, not by this copy step (the copy just places bytes at the right path). The story's responsibility is the byte-faithful copy at the verbatim path + the correct extension surviving; assert `Content-Type` in tests (AC1 `image/*`, AC5 `video/mp4`) against the PREVIEW server. Flag: if a fixture is misnamed (a PNG saved as `.jpg`), extension-based Content-Type will mislabel it — keep fixture extension honest (Task 4 already calls this out). Do NOT add a custom MIME map in this story unless the preview server lacks one for `.mp4` (verify; if `astro preview` serves `.mp4` as `application/octet-stream`, document it and scope the Content-Type assertion to what the deploy target — Azure SWA — actually returns). [Source: AC1/AC5 Content-Type; Azure SWA static MIME by extension]
    - [ ] **Empty `content/` subtrees / no assets at all:** if `content/` has no non-`.md` assets (only the `.gitkeep`), the hook must be a clean no-op (copy zero files, exit 0) — not an error on an empty/absent media dir. Guard a missing `content/media/` gracefully. [Source: AC7 build exits 0]
    - [ ] **Symlinks in `content/`:** if a vault asset is itself a symlink, do NOT follow it out of the vault (path-escape / arbitrary-file-read risk); copy the link target's bytes only if the target is inside `content/`, else skip + warn. (Defensive; vault assets are normally real files.) [Source: Decision 2 "content/ canonical"; path-escape parity with the `..`-escape guard]

- [ ] **Task 3 — Wire the media rehype plugin into the Astro markdown pipeline** (AC: 1, 2, 3, 4, 5, 7)
  - [ ] In `web/astro.config.mjs`, import the plugin (`import rehypeMdMedia from './src/lib/rehype-md-media.mjs';`) and add it to `markdown.rehypePlugins` ALONGSIDE the existing `rehypeMdLinks` (e.g. `rehypePlugins: [rehypeMdLinks, rehypeMdMedia]`). Order is independent (links touch `<a>`, media touches `<img>/<video>/<audio>/<source>` — disjoint node sets), but keep `rehypeMdLinks` first to preserve the established pipeline. Keep `gfm: true`, `remarkPlugins: [remarkGfm]`, and `shikiConfig.theme: 'github-light'` intact. [Source: web/astro.config.mjs lines 20–28; AC7]
  - [ ] Register the media-copy integration (Task 2) in the SAME `defineConfig` (add an `integrations: [copyVaultMedia()]` array, or inline the `{ name, hooks: { 'astro:build:done' } }` object). A rehype plugin alone cannot copy files; the integration hook is what gets the bytes into `dist`. Confirm both are wired: the rewrite (rehype) and the copy (integration). [Source: Astro integrations API; AC1]
  - [ ] Update the `astro.config.mjs` header comment to note Story 2.4 adds relative media `src`→served-URL rewriting (rehype) + a build-time vault-media copy into `dist` (integration), the web half of FR-3 (in addition to 2.1 render, 2.2 theme, 2.3 link rewriting). Confirm runtime stays JS-free: the rewrite is build-time, emitted HTML is plain `<img src="/…">`/`<video>`; the copy is build-time; no `client:*`, no runtime script. [Source: web/astro.config.mjs lines 6–19; FR-5/FR-7]

- [ ] **Task 4 — Add a media fixture (the real asset) + content that exercises the media cases (TDD-friendly)** (AC: 1, 2, 3, 4, 5, 6, 7)
  - [ ] Add the canonical epic asset: create `content/media/powder.jpg` (NEW — a tiny REAL image so `![](media/powder.jpg)` resolves to a live 200 served file with a non-zero rendered size). Keep it small (a few-hundred-byte valid JPEG/PNG is fine — a 1×1 or small placeholder). `content/media/` currently holds only `.gitkeep`; this is the first real asset. Ensure it is committed (binary), not git-ignored. If a JPEG is awkward to author, a small PNG named `powder.jpg` will NOT do (Content-Type sniffing/extension mismatch); use a real JPEG for `.jpg`, or pick a `.png` fixture name and use it consistently in the embed. [Source: epics.md Story 2.4 line 235 `![](media/powder.jpg)`; content/media/ tree (only .gitkeep today)]
  - [ ] Extend `content/x.md` with a dedicated **"Media (AC: 2.4)"** section (use `##`/`###`, NEVER a second `<h1>` — `ac1-gfm-core` asserts exactly one `<h1>` on `/x`) containing: the epic embed `![a skier in deep powder](media/powder.jpg)` (same-dir + alt text → AC1/AC4); an empty-alt embed `![](media/powder.jpg)` (AC4 `alt=""`); an external image `![remote](https://example.com/a.png)` (AC3 pass-through, NOT copied); a raw-HTML video `<video controls poster="media/powder.jpg"><source src="media/clip.mp4" type="video/mp4"></video>` (exercises `<source src>` rewrite AND `<video poster>` rewrite — AC5) OR document that video is exercised from a nested fixture; a **missing** asset `![broken](media/missing.jpg)` (AC6 — rewrites to `/media/missing.jpg`, 404s, no crash); and the **F1 smuggling fixtures** `![smuggle](media%2Fpowder.jpg)` + `![bad](bad%zz.jpg)` (AC2 guard — must stay UNREWRITTEN; table rows 12–14). Note: raw `<video>` HTML in markdown requires Astro/remark to PASS RAW HTML THROUGH — confirm the pipeline does not strip it (GFM + Astro default allows raw HTML; if a sanitizer drops it, the `<video>` case must move to where raw HTML survives, or document the constraint). Do NOT add a second `<table>` or a second `<h1>` (keep `ac1-gfm-core` / `2-2-theme` assertions on `/x` valid). [Source: AC1–AC6; web/tests/ac1-gfm-core.spec.ts (single-h1, single-table); content/x.md current state; Astro raw-HTML passthrough]
  - [ ] Add the **nested-page** + `..` resolution fixture: extend `content/sub/page.md` (page `/sub/page`) with `![from sub](../media/powder.jpg)` (→ `/media/powder.jpg`, proves `..` resolution against the page dir) and optionally a sibling asset `content/sub/diagram.png` referenced as `![sibling](diagram.png)` (→ `/sub/diagram.png`, proves same-nested-dir resolution + that the copy step preserves nested paths). Add the small real `content/sub/diagram.png` asset if you assert the sibling case. [Source: AC2; content/sub/page.md current state]
  - [ ] If asserting AC5 video serving end-to-end, add a tiny real `content/media/clip.mp4` (a few-KB valid mp4) so the `<source src>` rewrite + copy + `video/mp4` Content-Type can be verified. If a real mp4 is impractical in-repo, scope AC5's served-Content-Type assertion to a documented note and at minimum assert the `<source src>` rewrite to `/media/clip.mp4` from the static HTML (the rewrite is the load-bearing part; the copy mechanism is identical to images and is covered by AC1's served-asset assertion). Document the choice in the Dev Agent Record. [Source: AC5]
  - [ ] Do NOT introduce a slug collision or a second `<h1>` on any existing asserted page; verify the build still emits the same clean set of content pages (no new `.md` pages added unless intentional — fixtures here are media + edits to existing `.md`). [Source: web/src/pages/[...slug].astro duplicate-slug guard lines 47–59; AC7]

- [ ] **Task 5 — Add Playwright specs for media embedding + serving** (AC: 1, 2, 3, 4, 5, 6, 7)
  - [ ] Add a new spec `web/tests/2-4-media.spec.ts` (follow existing harness conventions — `import { test, expect } from '@playwright/test'`, `test.describe`, guard route assertions with a 200-status `beforeEach` like `ac3-crawlable-shell.spec.ts`). [Source: web/tests/ac3-crawlable-shell.spec.ts pattern]
  - [ ] **AC1 (inline render + served asset, build-time, JS-free):** load `/x`, assert the rendered HTML has `<img src="/media/powder.jpg">` (resolved served path, NOT the literal `media/powder.jpg`); read the `src` via `locator.getAttribute('src')` from static HTML (proves build-time rewrite, no JS). Then assert the asset is actually served: `const res = await page.request.get('/media/powder.jpg'); expect(res.status()).toBe(200); expect(res.headers()['content-type']).toMatch(/^image\//);`. Optionally assert the image has a non-zero natural size (`naturalWidth > 0`) to prove it actually loaded inline. [Source: AC1]
  - [ ] **AC2 (resolution correctness):** load `/sub/page` and assert its `![from sub](../media/powder.jpg)` → `src="/media/powder.jpg"` (parent `..` resolved against `/sub/page`'s dir), and (if added) the sibling `![sibling](diagram.png)` → `/sub/diagram.png` with that asset served 200. Assert filenames are NOT slugged (a verbatim-cased asset, if you add one, keeps its on-disk name). Read each assertion from rendered `getAttribute('src')` (build output), never from a re-implementation of the resolution fn (avoid the 2.2 tautological-test trap flagged in 2.3 Dev Notes). [Source: AC2; 2.3 Dev Notes "avoid tautological tests"]
  - [ ] **AC3 (pass-through):** assert the external image `![remote](https://example.com/a.png)` still has `src="https://example.com/a.png"` (untouched, NOT copied); assert no attempt rewrote/copied it (the `/a.png`-style path is not served). If a `data:`/protocol-relative/root-absolute fixture is added, assert each is unchanged. [Source: AC3]
  - [ ] **F1 smuggling guards (the `%2F` reuse — assert the encoded-separator cannot manufacture a path part):** add a fixture embed with an encoded separator and assert it is left UNREWRITTEN (verbatim `src`), NOT split/rewritten: `![smuggle](media%2Fpowder.jpg)` → `src` stays `media%2Fpowder.jpg` (table row 13), and a leading-`%2F` form → unchanged (row 14). Assert a malformed `%`-escape `![bad](bad%zz.jpg)` stays `bad%zz.jpg` (row 12, decode throws → degrade). These prove the per-segment-decode guard copied from `rehype-md-links.mjs` is active on the media path, not just the link path. Read from rendered `getAttribute('src')`. [Source: AC2; web/src/lib/rehype-md-links.mjs lines 130–160; table rows 12–14]
  - [ ] **AC4 (alt/attribute passthrough):** assert `<img src="/media/powder.jpg" alt="a skier in deep powder">` (alt preserved through the rewrite); assert the empty-alt embed emits `alt=""`. If a `title`/`width`/`height` is authored on a fixture, assert it survives. [Source: AC4]
  - [ ] **AC5 (non-image media served — cover the FULL element matrix):** assert the `<source src>` (or `<video src>`) → `/media/clip.mp4` in the static HTML; if the real mp4 fixture was added, assert `page.request.get('/media/clip.mp4')` → 200 with a `video/`-ish Content-Type. If mp4 was scoped out per Task 4, assert at least the rewrite and note the serving parity with images. Additionally, if a `<video poster="media/powder.jpg">` fixture is authored, assert the `poster` attribute ALSO rewrote to `/media/powder.jpg` (poster is a distinct attribute from `src` — prove it is independently rewritten, table coverage). If a `<audio src>` or a multi-`<source>` `<video>` fixture is authored, assert each child `<source src>` rewrote independently. [Source: AC5; media-element attribute matrix]
  - [ ] **Content-Type honesty caveat (testability):** read the Content-Type from the PREVIEW server (`page.request.get(...).headers()['content-type']`) — note in the spec that `astro preview` may serve `.mp4` differently than Azure SWA; if the preview server returns `application/octet-stream` for `.mp4`, relax the assertion to "served 200 with non-empty body" and document the deploy-target Content-Type expectation in a comment, rather than asserting a value the preview server does not actually emit (avoid a test that passes only by accident or fails on a harness quirk). [Source: Task 2 Content-Type-by-extension note; AC5]
  - [ ] **AC6 (missing asset → 404, never crash):** assert the missing embed `![broken](media/missing.jpg)` still rewrote to `<img src="/media/missing.jpg">` AND that `page.request.get('/media/missing.jpg')` → 404 (broken image, not a build crash, not a served 0-byte file). The fact the FULL suite builds at all (the `webServer` `npm run build` succeeds) proves the build did not crash on the missing reference. [Source: AC6]
  - [ ] **AC7 (no regression):** run the FULL suite `cd web && npx playwright test` and confirm all **66 prior specs** (`ac1`, `ac2`, `ac3`, `ac5`, `ac6`, `2-2-theme`, `2-3-linking-nav`) stay green alongside the new `2-4-media` specs. If any prior spec breaks (e.g. a second `<h1>`/`<table>` added to `/x`, or the 404/link behaviour changed), the media changes regressed 2.1/2.2/2.3 and must be corrected (2.4 is additive). [Source: 66/66 baseline; AC7]

- [ ] **Task 6 — Final verification against ACs (Definition of Done)** (AC: 1, 2, 3, 4, 5, 6, 7)
  - [ ] `cd web && npm run build` exits 0, emits one page per `content/**/*.md`, and `dist/media/powder.jpg` (and any other added asset) is present in the build output at its served path. [Source: AC1, AC7]
  - [ ] Relative image embeds in the emitted HTML are rewritten to served root-absolute paths (`![](media/powder.jpg)` on `/x` → `<img src="/media/powder.jpg">`), resolved against the current page's dir, with nested/`..`/`./`/encoded handling correct and filenames preserved byte-for-byte (AC1, AC2). [Source: AC1, AC2]
  - [ ] External/`data:`/protocol-relative/root-absolute media `src` left unchanged and not copied; `<a>` links remain Story 2.3's concern (AC3). [Source: AC3]
  - [ ] `alt`/`title`/other authored attributes survive the rewrite; empty alt → `alt=""` (AC4). [Source: AC4]
  - [ ] Relative `<video>`/`<audio>`/`<source>`/`poster` assets are rewritten + served the same way as images (AC5). [Source: AC5]
  - [ ] A missing referenced asset rewrites to its would-be served path and 404s (broken image), never crashing the build (AC6). [Source: AC6]
  - [ ] Page stays server-rendered, JS-free, semantically well-formed; single `<h1>`/`<main>`/`<article>`/doctype shell + 2.2 theme + 2.3 linking/404 intact; `cd web && npx playwright test` → all 66 prior specs + new media specs pass; `cd web && npx astro check` → 0 errors (AC7). [Source: AC7]
  - [ ] **Scope discipline held:** NO inter-file `.md` link rewriting changes (Story 2.3 — leave `rehype-md-links.mjs` and `<a href>` handling untouched), NO vault index / entry surface (Story 2.5), NO site-header / get-client-cta / pitch-card (Story 2.6), NO content negotiation / `api/` changes (Story 2.7), NO native-client / WPF MediaElement media (Epic 3 Story 3.5), NO `astro:assets`/`<Image>` optimisation pipeline or image transforms (the AC is "served from the vault path", a verbatim copy — not an optimiser), NO SPA/client-router/JS. This story is relative media `src` rewriting + vault-media serving ONLY. [Source: epics.md Epic 2 stories 2.5–2.7 lines 239–278, Story 3.5 line 338; EXPERIENCE.md line 77]

## Dev Notes

### What exists right now (read before coding)

- `web/` is the Astro **5.18.2** project from Stories 1.1 + 2.1 + 2.2 + 2.3. 2.1 shipped the render path (the `glob()` content collection over the repo-root `../content` vault, the `[...slug].astro` catch-all route with shared `pathToSlug`, the `Page.astro` shell). 2.2 layered the GitHub theme. 2.3 added relative-`.md`-link rewriting (`web/src/lib/rehype-md-links.mjs`) + a custom `web/src/pages/404.astro` + the shared `web/src/lib/slug.mjs`. **2.4 adds media `src` rewriting + vault-media serving** on top — it does NOT change routing, the content collection's page emission, the theme, or the link/404 behaviour. [Source: 2-1/2-2/2-3 story files; web/ tree]
- `web/astro.config.mjs` currently sets `markdown: { gfm: true, remarkPlugins: [remarkGfm], rehypePlugins: [rehypeMdLinks], shikiConfig: { theme: 'github-light' } }`. 2.4 adds `rehypeMdMedia` to `rehypePlugins` AND an `integrations: [...]` entry for the media-copy hook. [Source: web/astro.config.mjs lines 20–28]
- **`web/src/lib/rehype-md-links.mjs` is the direct template to mirror** — it already implements (a) the VFile→source-path resolution with degrade-never-crash (`resolveSourcePath`, lines 56–71, 76–90), (b) the page-dir-slug derivation (lines 100–103), (c) the pass-through guards (lines 112–119), (d) the per-segment decode + separator/scheme smuggling guards (F1, lines 130–160), (e) the `..`-escape + leading-`/` guards (lines 168–172), (f) the `console.warn`-on-degrade visibility (lines 84–88). The media plugin reuses ALL of this; the ONLY behavioural differences are: it visits `img`/`video`/`audio`/`source` (not `a`), rewrites `src`/`poster` (not `href`), only rewrites RELATIVE non-scheme paths (it does NOT gate on a `.md` extension — instead it leaves the asset path verbatim), and **does NOT `pathToSlug` the asset filename** (only resolves the directory; the file is served by its real on-disk name). [Source: web/src/lib/rehype-md-links.mjs full file]
- **`web/src/lib/slug.mjs`** exposes `pathToSlug(relPosixPath)` — used to derive the page's *directory* slug for resolution. Reuse it for the page-dir part ONLY; do NOT apply it to the asset's own path/filename. [Source: web/src/lib/slug.mjs]
- `web/src/content.config.ts` line 9 resolves the vault as `new URL('../../content', import.meta.url)` and the collection globs ONLY `**/*.md`. Non-`.md` assets are NOT collected and NOT in `dist` today — Task 2 fixes this. [Source: web/src/content.config.ts lines 9–13]
- `content/media/` exists but holds only `.gitkeep` — **no real asset yet**. Task 4 adds the first real media file (`content/media/powder.jpg`). [Source: content/media/ tree]
- `content/x.md` is the big GFM fixture (asserted by `ac1`/`ac6`/`2-2-theme` for exactly one `<h1>`, one `<table>`, GFM extensions, theme tokens). Adding a "Media" section must use `##`/`###` and must not add a second `<h1>` or a second `<table>`. [Source: content/x.md; web/tests/ac1-gfm-core.spec.ts]
- `content/sub/page.md` is the nested fixture (page `/sub/page`) already carrying 2.3 `..`/sibling LINK cases; add media `..`/sibling cases alongside. [Source: content/sub/page.md; 2-3 File List]

### The load-bearing decisions

**Decision 1 — rehype `<img>/<video>` `src` rewrite (NOT remark, NOT `astro:assets`).** At the rehype (HAST) stage, markdown image embeds are already `<img src="…">` elements; rewriting `properties.src` is a one-line mutation, exactly parallel to how 2.3 rewrites `<a href>`. A remark (MDAST) plugin would have to handle `image` and raw-HTML `<video>` nodes separately. `astro:assets`/`<Image>` is the WRONG tool: it is an image-OPTIMISATION pipeline (resize, format-convert, hashing) — the AC is "the asset is served from the vault path", i.e. a faithful verbatim copy, not an optimised/renamed variant. Architecture (line 104) says "resolve relative paths against the vault", and 2.3's precedent is a build-time rehype `href` rewrite — the media `src` rewrite is the exact analogue. [Source: architecture.md line 104; web/src/lib/rehype-md-links.mjs; Astro assets docs (out of scope)]

**Decision 2 — serve vault media by build-time copy into `dist` (NOT Astro `public/`, NOT symlink).** SWA serves from `web/dist` (deploy-web.yml line 59). The cleanest way to get `content/media/**` into `dist/media/**` without violating "`content/` is the single source of truth … No content in code" (architecture.md line 143) is an `astro:build:done` integration hook that copies non-`.md` vault files into the just-written `dist`. Putting media under `web/public/` would duplicate vault content into `web/` (architecture violation + desync risk); a symlink is fragile across CI/OS and Astro may not follow it. The copy is deterministic, keeps the vault canonical, and matches 2.3's "choose the simplest robust static approach" instinct. **Path invariant:** asset path relative to `content/`, verbatim, root-absolutised → both the copied `dist` path and the rewritten `src` agree. [Source: deploy-web.yml line 59; architecture.md line 143; Astro `astro:build:done` hook]

**Decision 3 — assets are NOT slugged; only the page-relative DIRECTORY is resolved.** This is the single most important divergence from 2.3. A `.md` link target gets `pathToSlug`'d (lower-cased, github-slugged) because it must match an emitted ROUTE. An asset must NOT be slugged: the served file is the literal on-disk file, so `src` must point at the real filename. Resolve `./`/`..`/nested-dir against the page dir (in POSIX space) but emit the asset's own segments verbatim. A slugged asset `src` would 404 (no file at the slugged name). [Source: AC2; web/src/lib/slug.mjs intended use]

### Resolution edge-case table (the AC2 contract)

All resolution is page-relative, POSIX forward-slash, **asset filename preserved verbatim** (no slugging). "Current page" is the page the embed is authored on.

| # | Current page | Authored `src` | Resolved served `src` | Rule |
|---|---|---|---|---|
| 1 | `/x` | `media/powder.jpg` | `/media/powder.jpg` | same-dir (epic example) |
| 2 | `/sub/page` | `../media/powder.jpg` | `/media/powder.jpg` | parent `..` resolved vs page dir |
| 3 | `/sub/page` | `pic.png` | `/sub/pic.png` | sibling in nested dir |
| 4 | `/x` | `./media/powder.jpg` | `/media/powder.jpg` | leading `./` collapse |
| 5 | `/x` | `media/Powder Day.JPG` | `/media/Powder%20Day.JPG` (or the exact on-disk name) | **filename NOT slugged**; case + spaces preserved |
| 6 | `/x` | `media/missing.jpg` | `/media/missing.jpg` (→ 404, no file) | missing asset still rewrites; 404s (AC6) |
| 7 | `/x` | `https://example.com/a.png` | unchanged | external scheme → pass-through (AC3) |
| 8 | `/x` | `data:image/png;base64,…` | unchanged | data URI → pass-through (AC3) |
| 9 | `/x` | `//host/a.png` | unchanged | protocol-relative → pass-through (AC3) |
| 10 | `/x` | `/already/served.png` | unchanged | root-absolute → pass-through (AC3) |
| 11 | `/x` | `../escape.png` | `../escape.png` (unrewritten) | `..` escapes vault root → leave as-is (no clamp) |
| 12 | `/x` | `bad%zz.jpg` | `bad%zz.jpg` (unrewritten) | malformed `%`-escape → decode fails → leave as-is, never throw |
| 13 | `/x` | `media%2Fpowder.jpg` | `media%2Fpowder.jpg` (unrewritten) | **F1 `%2F` smuggling guard:** a single segment that decodes to contain `/` (a manufactured separator) → leave UNREWRITTEN, never split into two path parts (mirror `rehype-md-links.mjs` line 146) |
| 14 | `/x` | `%2Fetc/passwd.png` | `%2Fetc/passwd.png` (unrewritten) | **F1 leading-`%2F`:** a segment decoding to a leading `/` (absolute / would emit protocol-relative `//`) → leave UNREWRITTEN (mirror line 158) |
| 15 | `/x` | `media/powder.jpg?v=2` | `/media/powder.jpg` (copied file = `powder.jpg`) | trailing `?query` stripped from the path used to RESOLVE + COPY; the served file is the path part only (the `?v=2` is a cache-buster on the URL, not part of the on-disk name) — but be defensive: if stripping the query leaves an empty path, leave unrewritten |
| 16 | `/x` | `media/powder.jpg#frag` | `media/powder.jpg#frag` (unrewritten OR `/media/powder.jpg` w/o frag) | a `#fragment` on a media `src` is non-standard/N/A for `<img>`; do NOT manufacture behaviour — treat the `#`-bearing value as not-a-clean-relative-path and leave UNREWRITTEN (documented), OR strip+rewrite the path part consistently with the query rule. Pick ONE and document in Dev Notes. (`<a href>` fragments are 2.3's concern, not media.) |
| 17 | `/x` | `media/POWDER.JPG` (on disk `powder.jpg`) | `/media/POWDER.JPG` (→ 404, no file) | **case-only mismatch:** the rewrite preserves the AUTHORED casing verbatim (no slug, no case-fold); if it does not byte-match the on-disk name, it 404s (AC6) — the rewrite does NOT case-correct. On a case-insensitive dev FS (macOS/Windows) the file may resolve locally but 404 on Azure SWA's case-sensitive serving — call this out so a green local build is not a false pass |
| 18 | `/x` | `media/résumé.png` (non-ASCII on disk) | `/media/r%C3%A9sum%C3%A9.png` (or exact encoded on-disk name) | non-ASCII filename: emit a URL-safe encoded `src` that, when the browser decodes it, byte-matches the on-disk UTF-8 name; the COPY uses the decoded on-disk name. Encode-on-emit / decode-on-copy must round-trip to the same bytes (deterministic) |
| 19 | `/x` | `media/` (trailing slash, no file) | `media/` (unrewritten) | a value resolving to a directory (empty basename) is not a servable asset → leave UNREWRITTEN, never copy a directory |

Rows 7–10 are AC3 pass-throughs (reuse 2.3's exact guards). Rows 11–12 reuse 2.3's `..`-escape + malformed-decode handling. **Rows 13–14 are the F1 `%2F`/leading-`%2F` smuggling guards reused verbatim from `rehype-md-links.mjs` lines 130–160 — the same encoded-separator risk applies to a media `src`, so a `%2F` that decodes to a manufactured separator/absolute path leaves the value UNREWRITTEN.** Row 15 is the trailing-`?query` strip-for-resolve rule; Row 16 is the `#fragment`-on-media decision (pick + document). Row 5 is the asset-specific NO-SLUG rule; Row 17 is its case-sensitivity corollary (verbatim casing → byte-match-or-404, and the **case-sensitive-Azure vs case-insensitive-dev-FS hazard**). Row 18 is the non-ASCII encode/decode round-trip. Row 19 is the directory/empty-basename guard. Row 6 is AC6. [Source: AC2/AC3/AC6; web/src/lib/rehype-md-links.mjs guards lines 130–160; Azure SWA case-sensitive static serving]

### The missing-asset / broken-image state (AC6)

Mirrors 2.3's "missing target → clear not-found, never a crash": the plugin rewrites `media/missing.jpg` → `/media/missing.jpg` without checking existence; the copy step copies only files that exist, so the browser gets a normal 404 for the asset (a broken-image placeholder), never a build crash. **Floor:** neither the rewrite nor the copy may throw on a missing reference. Recommended (not required): a build-time `console.warn` listing referenced-but-missing assets, mirroring `rehype-md-links.mjs`'s `unresolvedPages` warn — so a typo is visible, not silent. Do NOT hard-fail the build by default. [Source: AC6; web/src/lib/rehype-md-links.mjs lines 84–88]

**Two non-negotiable floors, two failure points:**
1. **Rewrite floor (rehype, per-page):** the `src` rewrite NEVER checks existence and NEVER throws — a missing asset rewrites to its would-be served path exactly like a present one. The rewrite cannot know what the copy step will/won't copy (rehype runs per-page; the copy is once-per-build, possibly after) so it must be existence-agnostic. A malformed `%`-escape or `%2F`-smuggle degrades to UNREWRITTEN (rows 12–14), which is a SEPARATE concern from "asset missing" — do not conflate.
2. **Copy floor (`astro:build:done`):** copies only files that EXIST in `content/`; a referenced-but-missing asset simply has no file → 404. The copy walks `content/` (what exists), NOT the set of referenced `src` (what's wanted), so a missing reference is structurally a no-op for the copy, never a lookup-that-throws.

**If implementing the recommended warn (the visibility nicety):** the rehype plugin may collect the SET of relative asset paths it rewrote (module-level `Set`, deduped, like `unresolvedPages`); the `astro:build:done` hook (which has `content/`'s real file list) can diff `referenced − present` and `console.warn` the missing ones ONCE (deduped across pages). This crosses the rehype↔integration boundary — keep it OPTIONAL and behind the floor: if the wiring is awkward, ship the floor (never-crash, 404) WITHOUT the warn and document the deferral. The warn must itself never throw (an empty/again-missing set is a clean no-op). The choice (warn vs no-warn vs fail) is recorded in the Dev Agent Record per AC6. [Source: AC6; web/src/lib/rehype-md-links.mjs lines 54, 84–88 `unresolvedPages` warn precedent]

### Regression guardrails — what 2.4 must NOT break (the 66 existing specs)

2.4 is additive (a media `src` rewrite + a build-time copy + media fixtures). It must not alter asserted structure/theme/links:
- `ac1-gfm-core.spec.ts` — exactly one `<h1>`, one `<table>` on `/x`. → The "Media" section uses `##`/`###`, adds no `<h1>`/`<table>`. (Markdown images render as `<img>` inside a `<p>` — no structural conflict.)
- `ac2-js-disabled.spec.ts` — content renders JS-disabled. → Media rewrite is build-time, copy is build-time; no JS added. Safe.
- `ac3-crawlable-shell.spec.ts` — single semantic shell, doctype, balanced tags. → No shell change; `<img>` lives inside `<article>`.
- `ac5-slugging-edge.spec.ts` — slug/title behaviour. → No new `.md` pages that collide slugs; media files are not pages.
- `ac6-gfm-extensions.spec.ts` — GFM extension assertions on `/x`. → Adding images does not remove/alter the existing GFM fixtures.
- `2-2-theme.spec.ts` — theme tokens/contrast on `/x`. → Theme untouched; images inherit existing CSS.
- `2-3-linking-nav.spec.ts` — link rewriting + 404. → Do NOT touch `rehype-md-links.mjs`, `404.astro`, or `<a href>` handling; the media plugin only touches media elements. Verify the existing `[a pdf](report.pdf)` non-`.md` LINK still passes through unchanged (it is an `<a>`, not media, so the media plugin ignores it). [Source: web/tests/*.spec.ts — 7 spec files, 66 tests, all green per sprint-status 2-3]

### Previous story intelligence (Stories 2.1–2.3)

- **Exit-0 build with a broken page is a FAILED gate** (2.1). Here: a build that exits 0 but emits `src` that 404 (no copy step), or copies to the wrong path, is a failed gate. Assert the *rendered `src`* AND the *served asset status*, not just exit code. [Source: 2-1/2-2/2-3 Dev Notes]
- `playwright.config.ts` runs `npm run build && npm run preview` with `reuseExistingServer: false` — every test run rebuilds fresh `dist/`, so the rewrite + copy take effect automatically; assert against the live preview (read `getAttribute('src')` and `page.request.get(...)`). [Source: web/playwright.config.ts]
- **Avoid tautological tests** (flagged in 2.2 review, heeded in 2.3): assert `src`/asset-status from the ACTUAL rendered HTML + real HTTP response, never against a re-implementation of the resolution fn. [Source: 2-2 Review Findings; 2-3 Dev Notes]
- **No new top-level dependency** — reuse `unist-util-visit` (resolvable, used by the link plugin), `github-slugger` (via `slug.mjs`, for the page-dir only), and `node:fs`/`node:path`/`node:url`. If a dep is genuinely needed, commit `package-lock.json`. [Source: 2-1/2-3 Dev Notes]
- **Anti-drift via shared module** (2.3 extracted `slug.mjs` so route + link plugin can't diverge). Apply the same instinct: factor the VFile→pageDirSlug helper (and `contentDir`) so the media and link plugins resolve the page dir identically. [Source: 2-3 Completion Notes "Slug-module extraction"]
- Bash tool resets cwd between calls — chain `cd web && …` or use absolute paths. [Source: 2-1/2-2/2-3 Dev Notes]

### Architecture compliance / guardrails

- **`content/` is the single source of truth (architecture.md line 143):** the build-time copy reads from `content/`; do NOT duplicate media into `web/`. [Source: architecture.md line 143]
- **Media resolution = web build-time, native render-time (architecture.md line 63, 104):** the web copies+rewrites at build; the native client (Epic 3 Story 3.5) resolves the same relative paths against the vault at render time. Keep the resolution rule documented so the two paths stay consistent. Web half ONLY here. [Source: architecture.md lines 63, 104; epics.md Story 3.5 line 338]
- **Born-compat / SEO (FR-7, NFR-4):** rewritten `src` are real `<img src="/served">` in static HTML — crawler-visible, JS-free. No JS image loading. [Source: epics.md FR-7/NFR-4]
- **Beauty + performance budget (NFR-3):** rewrite + copy are build-time; runtime stays static, zero-JS. No image-optimisation pipeline (out of scope) — a faithful served copy. [Source: epics.md NFR-3]
- **Don't reinvent plumbing (NFR-7):** use the unified/rehype stack Astro already runs + a stdlib file copy in a standard `astro:build:done` hook — no custom asset framework. [Source: epics.md NFR-7; architecture.md line 70]
- **Accessibility floor (NFR-6, UX-DR9):** alt text passes through unchanged (AC4). [Source: epics.md NFR-6; UX-DR9]

### Scope boundaries — what this story is NOT (prevent scope creep)

This story is **relative media `src`/`poster` rewriting + serving vault media on the WEB path** ONLY. Explicitly OTHER stories — do not pull in:
- **Story 2.3** — `.md` inter-file link rewriting + 404. Do NOT modify `rehype-md-links.mjs`, `404.astro`, or `<a href>` handling. The media plugin touches media elements only. [Source: epics.md Story 2.3 line 213]
- **Story 2.5** — browsable vault index / entry surface. Do not build an index. [Source: epics.md Story 2.5 line 239]
- **Story 2.6** — `site-header` + `pitch-card` + EXPERIENCE microcopy. [Source: epics.md Story 2.6 line 252]
- **Story 2.7** — content negotiation (`Accept` → raw `.md`) in `api/`. Do not touch `api/`. [Source: epics.md Story 2.7 line 266]
- **Epic 3 Story 3.5** — native-client (WPF `MediaElement`/`Image`) media. Web-only here; EXPERIENCE.md confirms native video via MediaElement is the native concern. [Source: epics.md Story 3.5 line 338; architecture.md line 104]
- **`astro:assets` / `<Image>` optimisation, responsive `srcset`, format conversion, image hashing/resizing** — the AC is a faithful served copy "from the vault path", NOT an optimiser. Do NOT add. [Source: AC1; Astro assets out of scope]
- **Client router / View Transitions / SPA / any runtime JS** — would break JS-free + crawlable. Do NOT add. [Source: FR-5/FR-7; ac2-js-disabled.spec.ts]
[Source: epics.md Epic 2 stories 2.3/2.5–2.7; EXPERIENCE.md line 77; architecture.md lines 63, 104, 143]

### Testing standards summary

- Verification: `cd web && npx playwright test` (run the FULL suite — prove 66 prior + new media specs pass). Typecheck: `cd web && npx astro check`. Build: `cd web && npm run build`. No lint command configured. [Source: 2-1/2-2/2-3 Dev Notes]
- Test the **built/preview output** (the Playwright config builds + previews `dist/`). Read `src` via `locator.getAttribute('src')`; assert serving via `page.request.get('/media/…')` → status + `content-type`; assert missing → 404. [Source: web/playwright.config.ts; ac3-crawlable-shell.spec.ts pattern]
- The harness rebuilds fresh each run (`reuseExistingServer:false`), so the rewrite + copy are always exercised against the latest build. [Source: web/playwright.config.ts]
- **Case-sensitivity false-pass hazard:** the deploy target (Azure SWA) and Linux CI serve assets case-SENSITIVELY; a macOS/Windows dev FS is case-INSENSITIVE. An asset embed whose authored casing does not byte-match the on-disk name (`media/Powder.JPG` vs disk `powder.jpg`) may serve 200 locally but 404 on deploy. Keep fixtures byte-exact (embed casing == on-disk casing) so the suite is not green-locally-but-broken-on-Azure; CI (Linux) is the source of truth. [Source: table row 17; deploy-web.yml Linux runner; Azure SWA case-sensitive serving]

### Project Structure Notes

- New/changed files expected:
  - `web/src/lib/rehype-md-media.mjs` (NEW — the media `src`/`poster` rewrite plugin, sibling to `rehype-md-links.mjs`).
  - `web/src/lib/copy-vault-media.mjs` (NEW — optional: the `astro:build:done` copy integration, if not inlined into `astro.config.mjs`).
  - `web/src/lib/page-path.mjs` (NEW — optional: extracted shared VFile→pageDirSlug + `contentDir` helper, imported by BOTH rehype plugins to prevent drift) — OR factor into `web/src/lib/slug.mjs`.
  - `web/astro.config.mjs` (UPDATE — add `rehypeMdMedia` to `rehypePlugins`, add the media-copy `integrations` entry, refresh header comment).
  - `content/media/powder.jpg` (NEW — the real epic asset fixture, binary, committed), and optionally `content/sub/diagram.png` / `content/media/clip.mp4` for AC2/AC5.
  - `content/x.md` (UPDATE — add "Media (AC: 2.4)" section), `content/sub/page.md` (UPDATE — add `..`/sibling media cases).
  - `web/tests/2-4-media.spec.ts` (NEW — media embedding + serving specs).
- Do NOT modify `web/src/lib/rehype-md-links.mjs` (2.3), `web/src/pages/404.astro` (2.3), `web/src/pages/[...slug].astro` / `content.config.ts` (route/collection — unless extracting a shared helper that those already use), `web/src/styles/github.css` (2.2 theme), `api/`, `clients/`, or `_bmad/`. [Source: architecture.md lines 119–143; web/ tree]
- No conflict with the established layout — media resolution is the named FR-3 web responsibility (architecture #Cross-Cutting 4 line 63, #Rendering Pipeline line 104). [Source: architecture.md lines 63, 104]

### References

- [Source: _bmad-output/planning-artifacts/epics.md#Story 2.4: Media embedding] — user story + AC (lines 227–237): `![](media/powder.jpg)` in a page with the asset in the vault → image appears inline AND the asset is served from the vault path (FR-3)
- [Source: _bmad-output/planning-artifacts/epics.md#Requirements Inventory] — FR-3 media embedding "relatively-referenced images/video served alongside content" (line 22); FR-5 server-rendered (line 24); FR-7 crawlable (line 26); NFR-3 beauty+perf (line 43); NFR-6 accessibility floor (line 46); NFR-7 don't reinvent plumbing (line 47); UX-DR9 accessibility (line 72)
- [Source: _bmad-output/planning-artifacts/ux-designs/ux-the-markdown-web-2026-06-21/EXPERIENCE.md] — "working links and inline media" on the web publish path (line 77); web is the agentless full experience; native MediaElement video is the native concern (Theo flow line 74)
- [Source: _bmad-output/planning-artifacts/architecture.md] — remark/rehype (GFM) browser path (line 70); media resolution build-time (HTML) vs render-time (native) (#Cross-Cutting 4, line 63); Images/media (FR-3): resolve relative paths against the vault, video via MediaElement later (#Rendering Pipeline, line 104); `content/` is the single source of truth, no content in code (line 143); monorepo layout incl. `content/media/` (lines 113–139)
- [Source: web/src/lib/rehype-md-links.mjs] — the sibling rehype pattern to mirror: VFile→source-path resolve + degrade-never-crash (56–90), page-dir-slug derivation (100–103), pass-through guards (112–119), per-segment decode + smuggling guards (130–160), `..`-escape/leading-`/` guards (168–172), warn-on-degrade (84–88)
- [Source: web/src/lib/slug.mjs] — `pathToSlug` (reuse for the page DIRECTORY only; do NOT slug asset filenames)
- [Source: web/src/content.config.ts] — content collection globs `**/*.md` only (non-`.md` assets are NOT collected — Task 2 must copy them); `../../content` vault base resolution (line 9)
- [Source: web/astro.config.mjs] — current markdown config (gfm + remark-gfm + rehypeMdLinks + shiki github-light), lines 20–28 — add `rehypeMdMedia` + `integrations`
- [Source: .github/workflows/deploy-web.yml] — `app_location: 'web/dist'`, `skip_app_build: true` (line 59): the deployed artifact root SWA serves from — media must land inside `web/dist`
- [Source: web/playwright.config.ts; web/tests/ac3-crawlable-shell.spec.ts] — harness (build+preview, reuse:false) + spec conventions (200-guarded beforeEach)
- [Source: _bmad-output/implementation-artifacts/2-1-render-a-md-file-to-an-html-page.md, 2-2-apply-the-github-style-default-theme.md, 2-3-inter-file-linking-and-navigation.md] — render path, Page shell, shared `slug.mjs`/anti-drift, the `rehype-md-links.mjs` template, 66-spec harness, "exit-0 with bad output = failed gate", avoid tautological tests

## Dev Agent Record

### Agent Model Used

{{agent_model_name_version}}

### Debug Log References

### Completion Notes List

### File List
