# AC Trace Report — Story 1.3: Deploy placeholder page via GitHub Actions on push (FR-17)

Generated by the enhanced-automated-sprint pipeline (Step 10). CI/CD story — verification is a real
GitHub Actions run + live HTTPS assertion.

## AC → Verification matrix

| AC | Requirement | Verification | Result |
|----|-------------|--------------|--------|
| AC1 | `deploy-web.yml` exists; push-to-main (path-filtered) + workflow_dispatch; no `pull_request` | File present; YAML lint confirms triggers `['push','workflow_dispatch']`, paths `['web/**', '.github/workflows/deploy-web.yml']` | ✅ PASS |
| AC2 | Deterministic build-then-deploy of `web/dist` via token | Ordered: checkout → setup-node 20 → `npm ci` → `npm run build` → `static-web-apps-deploy@<sha>` (`skip_app_build`, `app_location: web/dist`, `output_location: ''`); run 27892801957 success | ✅ PASS |
| AC3 | Failed build does NOT deploy | Structural: build precedes deploy in one job, no `continue-on-error`/`if: always()` → fail-fast aborts before upload (not destructively tested, to protect the live site) | ✅ PASS |
| AC4 | Token stored as secret, never leaked | `AZURE_STATIC_WEB_APPS_API_TOKEN` set via piped `gh secret set` (never printed); referenced only as `${{ secrets.* }}`; no `pull_request` fork path; `permissions: contents: read` | ✅ PASS |
| AC5 | Push deploys, no manual steps; live over HTTPS serving OUR page | Push → run 27892649594 success (1m14s); `curl https://purple-pond-09fadd20f.7.azurestaticapps.net/` → HTTP 200, body contains "The Markdown Web" + "Coming soon" (our 265-byte page) | ✅ PASS |

## Live evidence
- Two successful runs: 27892649594 (initial), 27892801957 (post-review-fix). Both green.
- Live URL serves the Astro placeholder over HTTPS with a valid certificate.

## Coverage gaps
- None for the 5 ACs. Custom domain (themarkdownweb.com) is Story 1.4.
- Residual CI annotation: GitHub Node-20 runner deprecation — platform-level, not actionable by our config.

## Code review
- PASS WITH ITEMS (0 critical). All patch/decision items fixed; 3 low items deferred with documentation.

**Trace verdict: 5/5 ACs satisfied. Publish-on-push (FR-17) is live.**
