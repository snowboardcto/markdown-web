# UX Decision Log — The Markdown Web

- 2026-06-21: Intent = CREATE. Inherited sources: PRD, architecture, brief, manifesto. Form-factor (from architecture): multi-surface — themarkdownweb.com (Astro browser site) + Windows native client (WPF). Core rendering = GFM, GitHub-style bedrock; AI personalities re-render per reader (later).
- 2026-06-21: UX direction (naethyn):
  - Native client look = the AI personality renders it; NO fixed visual identity to pre-design (intentionally open).
  - HTML/browser path = render BASIC — clean, minimal, GitHub-ish; no heavy branding.
  - References: none. Brand: blank slate (no personality/color/wordmark yet).
- 2026-06-21: Created example `.working/basic-html-render.html` + screenshot `.working/basic-html-render.png` — a basic HTML render of a sample GFM page (headings, bold/italic/strike, inline+fenced code w/ light highlighting, blockquote, task list, GFM table, ordered list, inline image, links, thin themarkdownweb.com top bar). For naethyn review.
- 2026-06-21: Mocks v2 created + screenshotted (.working/):
  - `web-v2`: removed fake browser chrome (browser shows URL). Web page = looks good + recruits: sticky site header (wordmark `.md the markdown web` + "the vision" + "Get the client" CTA) and an end-of-page PITCH card casting the vision + "Get the Markdown Web client". Web's job = render well AND share vision / sell the native client (the upgrade moment).
  - `native-client`: desktop window shell — titlebar, toolbar with back/fwd/reload + ADDRESS BAR showing `themarkdownweb.com/guides/powder-day.md` with a ".md only" tag (loads .md only), + a PERSONALITY selector ("Cozy Reader"). Content rendered by an illustrative AI personality (warm serif "Cozy Reader" + a personality-generated TL;DR card) to show why you'd want the client. Native look = personality-driven (illustrative only; not a fixed design). Light/dark = personality/client-dependent.
- 2026-06-21: FINALIZED. Distilled DESIGN.md (web theme + native shell chrome; native content = personality, undesigned) and EXPERIENCE.md (multi-surface foundation, IA, voice, components incl. .md-only address bar + personality selector, states, interactions, a11y floor, 3 key flows KF-1..3). Promoted 3 mocks to mockups/. Both spines status=final. Reviewer Gate offered as optional (not run — solo/early stage; available via Validate later).
